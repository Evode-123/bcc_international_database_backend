# PostgreSQL connection string
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=evode2002@
DB_NAME=bcc_international_database_db
# ─── Server ───────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# Rename these to match what env.ts expects:
JWT_ACCESS_SECRET="3f8a2c1d9e4b7f6a0c5d2e8b1f4a9c3e7d0b5f2a8e1c6d3b9f7a4e2c0d8b5f1"
JWT_ACCESS_EXPIRES=7d

# Auto-seeded super_admin -- created automatically on first server start.
# Change these before the FIRST run. After that, log in and change the
# password immediately (the system will force this anyway).
SUPER_ADMIN_EMAIL="evodeniyitegeka10@gmail.com"
SUPER_ADMIN_TEMP_PASSWORD="ChangeMe123!"
SUPER_ADMIN_FULL_NAME="Super Admin"

# SMTP -- used to email temporary passwords to newly invited admin users.
# Leave SMTP_HOST blank during local development: emails will be logged
# to the console instead of actually sent.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=niyitegekaevode4@gmail.com
SMTP_PASS=eaeekphprhhztykp
SMTP_FROM="BCC Training <niyitegekaevode4@gmail.com>"